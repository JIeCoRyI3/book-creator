{
  "name": "book-builder",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently -n server,client -c blue,green \"npm run -w server dev\" \"npm run -w client dev\"",
    "build": "npm run -w server build --if-present && npm run -w client build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
